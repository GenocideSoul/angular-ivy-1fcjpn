export class Shows {
  constructor(public id: number, public title: string) {}
  ersetzt;
  public id: number;
  public title: string;
  constructor(id: number, title: string) {
    this.id = id;
    this.title = title;
  }
}
